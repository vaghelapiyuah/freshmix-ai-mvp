streamlit>=1.36
pydantic>=2.6
python-dotenv>=1.0

# Phase 3 — HTTP API boundary + client
fastapi>=0.110
uvicorn>=0.29
httpx>=0.27

# Optional: enables the Claude agent path (intent parse + rationale).
anthropic>=0.92.0

# Dev
pytest>=8.0
